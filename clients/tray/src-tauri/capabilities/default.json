{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Popover window capabilities (read-only surface)",
  "windows": ["console"],
  "permissions": [
    "core:default",
    "core:event:default",
    "notification:default",
    "clipboard-manager:allow-write-text",
    "opener:allow-open-url"
  ]
}
